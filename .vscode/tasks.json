{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile and Run from linkedList /",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "cd linkedList && g++ 1d.cpp -o main && ./main"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
